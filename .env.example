# ──────────── MLflow Settings ────────────
# URI for your MLflow tracking server
MLFLOW_TRACKING_URI=http://localhost:5000
# MLflow experiment name
MLFLOW_EXPERIMENT=FluxPilot_Granite

# ──────────── Base Model ────────────
# Hugging Face model path (Granite LLM)
BASE_MODEL=granite/granite-model

# ──────────── Training Hyperparameters ────────────
# Number of fine-tuning epochs
EPOCHS=3
# Batch size per device
BATCH_SIZE=4

# ──────────── AWS / CI-CD Settings ────────────
# (These would go into GitHub Secrets instead of .env for CI/CD)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_ACCOUNT_ID=
# AWS_REGION=us-west-2
# ECR_REPOSITORY=fluxpilot
# SAGEMAKER_ENDPOINT_NAME=fluxpilot-endpoint
# SAGEMAKER_EXECUTION_ROLE_ARN=arn:aws:iam::123456789012:role/fluxpilot-sagemaker-exec-role
